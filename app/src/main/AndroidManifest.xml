<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.projectvaragari">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Single_User_Profile_Activity"></activity>
        <activity android:name=".User_Profile_Activity" />
        <activity android:name=".User_Show_Activity" />
        <activity android:name=".UserVaraActivity" />
        <activity android:name=".GariUpdateDelete" />
        <activity android:name=".AdminActivity" /> <!-- <activity android:name=".AdminActivity" /> -->
        <activity android:name=".GariVaraDiboActivity" />
        <activity android:name=".GariVaraNiboActivity" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".SignUpActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>